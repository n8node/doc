# Проверка раздела «Генерация изображений»

## Что сделано

1. **БД**: таблица `image_generation_tasks` (миграция `20260317180000_add_image_generation_tasks`).
2. **Конфиг**: ключи AdminConfig — `kie.api_key`, `generation.image_enabled`, `generation.image_tasks`, `generation.image_models`, `generation.margin_percent` (наценка на кредиты отдельно от маркетплейса LLM).
3. **Бэкенд**: клиент Kie (4o Image, Flux Kontext), создание задачи, опрос статуса, webhook; автосохранение результата на диск пользователя (S3 + запись File).
4. **API**:  
   - `POST /api/v1/generate/image` — запуск генерации  
   - `GET /api/v1/generate/image/status?taskId=` — статус и результат  
   - `GET /api/v1/generate/image/tasks` — список задач  
   - `GET /api/v1/generate/image/models?taskId=` — модели по задаче  
   - `GET /api/v1/files/:id/generation-url` — временный URL файла для Kie  
   - `POST /api/v1/webhooks/kie-image` — callback от Kie (без авторизации)  
   - Админ: `GET/PUT /api/v1/admin/generation/config`, `GET/POST /api/v1/admin/kie/api-key`, `GET /api/v1/admin/kie/status`
5. **Админка**: страница «Генерация изображений» — API-ключ Kie, вкл/выкл раздела, наценка %, задачи и модели. В форме тарифа добавлена фича «Генерация изображений (Kie.ai)» (`content_generation`).
6. **Кабинет**: страница `/dashboard/generate/image` — выбор задачи → модели → промпт (и при необходимости изображения из «Мои файлы») → отправка → опрос статуса → результат (картинка, стоимость в кредитах, ссылка на файл на диске).

## Как проверить

### 1. Миграция (на сервере или локально с поднятой БД)

```bash
# Применить миграцию
docker exec -i dropbox-ru-db-1 psql -U postgres -d dropbox_ru < prisma/migrations/20260317180000_add_image_generation_tasks/migration.sql

# или локально
npx prisma migrate deploy
```

### 2. Админка

- Войти как админ → в сайдбаре пункт **«Генерация изображений»**.
- Задать **API-ключ Kie.ai** (получить на https://kie.ai/api-key), нажать «Сохранить ключ», затем «Проверить соединение» — должно быть «Подключение успешно».
- Включить **«Генерация изображений включена»**, задать **наценку %** (например 10).
- Сохранить **задачи и модели** (по умолчанию уже заполнены).
- В **Тарифы** для нужного плана включить фичу **«Генерация изображений (Kie.ai)`**.

### 3. Кабинет пользователя

- Войти под пользователем с тарифом, у которого включена «Генерация изображений».
- В разделе «Генерация» открыть **«Изображения»** (или перейти по `/dashboard/generate/image`).
- Выбрать задачу (например «Генерация по описанию»), модель (4o Image или Flux Kontext), ввести промпт (например `A sunset over the sea`).
- Нажать **«Сгенерировать»**. Должен появиться блок «Результат» с индикатором загрузки, затем — картинка и строка «Стоимость: X кредитов».
- Убедиться, что в **«Мои файлы»** появился новый файл (автосохранение с именем вида `generated-xxxx.png`).

### 4. Редактирование / вариации

- Загрузить любое изображение в «Мои файлы».
- На странице генерации выбрать задачу «Редактирование по промпту» или «Вариации по образцу», выбрать модель, в блоке «Исходное изображение» выбрать загруженный файл, ввести промпт.
- Запустить генерацию и проверить результат и появление файла в «Мои файлы».

### 5. Webhook (если Kie вызывает callback)

- Убедиться, что в окружении задан корректный публичный `APP_URL` (или `NEXTAUTH_URL`), чтобы `callBackUrl` в запросе к Kie был доступен с интернета (например `https://your-domain.com/api/v1/webhooks/kie-image`).
- После успешной генерации задача должна перейти в статус `success`, в ней заполнены `resultUrl` и `fileId` (файл сохранён на диск).

## Переменные окружения

- `CONFIG_ENCRYPTION_KEY` — для хранения зашифрованного ключа Kie (как для других ключей в AdminConfig).
- `APP_URL` или `NEXTAUTH_URL` — базовый URL приложения для формирования `callBackUrl` при вызове Kie.

## Ограничения

- Транскрипция и прочие сервисы Kie в этом разделе не задействованы; добавлены только генерация/редактирование изображений (4o Image, Flux Kontext).
- Стоимость в кредитах отображается по данным, которые возвращает Kie (в callback или при опросе recordInfo); при отсутствии поля стоимости показывается 0 или «стоимость не указана».
