---
description: Деплой dropbox-ru — синхронизация гита, миграции и пересборка Docker
alwaysApply: true
---

# Деплой dropbox-ru на сервер

Команды выполняются в **Termius (SSH)** на удалённом сервере в указанном порядке.

## 1. Синхронизация Git

Обновить код из репозитория:

```bash
cd /opt/dropbox-ru && git pull origin main
```

## 2. Миграция базы данных

Применить SQL-миграцию к PostgreSQL (замени путь на актуальную миграцию при необходимости):

```bash
docker exec -i dropbox-ru-db-1 psql -U postgres -d dropbox_ru < /opt/dropbox-ru/prisma/migrations/20260314120000_add_support_tickets/migration.sql
```

Для других миграций подставь путь: `/opt/dropbox-ru/prisma/migrations/<имя_миграции>/migration.sql`.

После миграции support_tickets выполни seed для тем тикетов (см. seed-dropbox-ru).

## 3. Пересборка образа и запуск

Пересобрать образы `app` и `docling`, затем перезапустить контейнеры:

```bash
docker compose build docling app && docker compose up -d
```

Если меняли только фронт/бэкенд (не docling):
```bash
docker compose build app && docker compose up -d
```

## ONLYOFFICE: в логах `app` нет `[onlyoffice document] incoming` при открытии редактора

Если ручной `wget` из контейнера `onlyoffice` на `http://app:3000/api/onlyoffice/document/...` с валидным Bearer даёт 200, а при открытии файла в браузере запросов к `/api/onlyoffice/document` нет:

1. После `git pull` на сервере выполнить: `sh docker/onlyoffice/apply-request-filter.sh` (включает в DS `request-filtering-agent.allowPrivateIPAddress` в `local.json`; см. ONLYOFFICE DocumentServer issue #2268).
2. Либо задать в `.env` приложения `ONLYOFFICE_DOCUMENT_DOWNLOAD_BASE_URL=https://<домен>` и в системном nginx добавить `location /api/` → порт приложения (см. `docker/nginx/host-ubuntu-onlyoffice.example.conf`).

---

## Секреты и деплой (VK, шифрование)

Защищённый ключ VK и другие секреты в админке хранятся в БД **зашифрованными** полем `CONFIG_ENCRYPTION_KEY` из `.env` на сервере. Если после `build` пропал вход через VK с сообщением «не подключён»:

1. Убедитесь, что в **`/opt/dropbox-ru/.env`** задан **тот же** `CONFIG_ENCRYPTION_KEY` (≥32 символа), что использовался при сохранении настроек в админке — ключ нельзя «перегенерировать» при каждом деплое.
2. Опционально добавьте в `.env` **`VK_CLIENT_ID`** и **`VK_CLIENT_SECRET`** (они уже попадают в контейнер через `env_file`) — приложение использует их, если запись в БД не расшифровывается.
3. Если в админке случайно снята галочка «Вход через VK», а ключи только в `.env`: **`VK_OAUTH_ENABLED=true`** — принудительно включает VK при наличии id/secret.
4. После деплоя провайдер VK подхватывается при старте и при каждом запросе к `/api/auth/*` (исправление сбоя, когда `instrumentation` не вызывал `refreshAuthOptions`).

**Порядок**: сначала `git pull`, затем миграция (если есть), затем `build` и `up -d`.
