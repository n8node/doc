# Dropbox-подобный сервис для qoqon.ru
# PostgreSQL 16 с pgvector для AI-поиска

services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: dropbox_ru
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./docker/db-init:/docker-entrypoint-initdb.d:ro
    command: >
      postgres
      -c shared_preload_libraries=vector
      -c max_connections=200
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    restart: unless-stopped

  n8n-db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: n8n_db
      POSTGRES_USER: n8n_admin
      POSTGRES_PASSWORD: ${N8N_DB_PASSWORD:-n8n_change_me}
    ports:
      - "5433:5432"
    volumes:
      - n8n_db_data:/var/lib/postgresql/data
    command: >
      postgres
      -c shared_preload_libraries=vector
      -c max_connections=100
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U n8n_admin -d n8n_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    ports:
      - "3000:3000"
    depends_on:
      db: { condition: service_healthy }
      docling: { condition: service_healthy }
    environment:
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@db:5432/dropbox_ru?schema=public
      NEXTAUTH_URL: ${NEXTAUTH_URL:-https://qoqon.ru}
      APP_URL: ${APP_URL:-https://qoqon.ru}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      REDIS_URL: redis://redis:6379
      DOCLING_URL: http://docling:8000
      N8N_DB_URL: ${N8N_DB_URL:-postgresql://n8n_admin:n8n_change_me@n8n-db:5432/n8n_db}
    deploy:
      resources:
        limits: { memory: 2G }
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro
    depends_on:
      - app
      - wordpress
    restart: unless-stopped

  docling:
    build:
      context: ./docker/docling
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      MAX_FILE_SIZE_MB: ${DOCLING_MAX_FILE_SIZE_MB:-100}
    deploy:
      resources:
        limits: { memory: 8G }
        reservations: { memory: 4G }
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped

  adminer:
    image: adminer:latest
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    depends_on:
      - db
    restart: unless-stopped

  # База данных только для WordPress (отдельно от PostgreSQL проекта)
  wordpress-db:
    image: mariadb:10.11
    environment:
      MYSQL_DATABASE: ${WP_DB_NAME:-wordpress}
      MYSQL_USER: ${WP_DB_USER:-wp_user}
      MYSQL_PASSWORD: ${WP_DB_PASSWORD:-wp_change_me}
      MYSQL_ROOT_PASSWORD: ${WP_DB_ROOT_PASSWORD:-wp_root_change_me}
    volumes:
      - wordpress_db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  wordpress:
    image: wordpress:6.4-php8.2-apache
    environment:
      WORDPRESS_DB_HOST: wordpress-db:3306
      WORDPRESS_DB_NAME: ${WP_DB_NAME:-wordpress}
      WORDPRESS_DB_USER: ${WP_DB_USER:-wp_user}
      WORDPRESS_DB_PASSWORD: ${WP_DB_PASSWORD:-wp_change_me}
      WORDPRESS_TABLE_PREFIX: wp_
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_HOME', '${WP_HOME:-https://qoqon.ru}');
        define('WP_SITEURL', '${WP_SITEURL:-https://qoqon.ru}');
    volumes:
      - wordpress_data:/var/www/html
    depends_on:
      wordpress-db: { condition: service_healthy }
    restart: unless-stopped

volumes:
  pg_data:
  redis_data:
  n8n_db_data:
  wordpress_db_data:
  wordpress_data:
